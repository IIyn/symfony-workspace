# symfony-workspace

## Run the project with Docker:

Just run

```
docker-compose up
```

or

```
docker-compose up -d
```

and you should be able to code without having php in your machine
